name: Testing

on:
  push:
    branches:
      - main
    paths: 
      - 'configurations/**'
jobs:
  checks:
    name: Select Environment
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.actor }}
      
